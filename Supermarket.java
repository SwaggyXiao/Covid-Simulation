import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)
/**
 * Supermarket class
 * 
 * A facility that can accomodate people.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Supermarket extends Facilities
{
    
    /**
     * Constructor of Supermarket that set the initial stats and image
     * for supermarket.
     */
    public Supermarket(){
    facilityImage = new GreenfootImage("Mall.png");
    drawImage();
    Area=800;
        infectionRate=1.2;
    }
    /**
    public void Entrance(boolean healthy){
        if(numOfPeople < maxPeople){
            //if(isTouching(Human.Class)){
            numOfPeople++;
                if(!healthy){
                    infectedPeople++;
                }
            //}
        } 
    }
    **/
    
     
    /**
     * Act - do whatever the Supermarket wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
      
     
}
